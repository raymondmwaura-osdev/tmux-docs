# `<variable name>` (e.g `pane_current_path`)

## Overview

* Provide a concise, formal description of the variable’s function within `tmux`.
* Explain its role in session, window, or pane management.

* State the default value assigned by `tmux`.
* Include notes on any version-specific differences.

* **scope**: Indicate whether the variable is global, session-specific, window-specific, or pane-specific.

---

## Accepted Values / Data Type

* Specify the data type (e.g., string, integer, boolean).
* List acceptable values, including constraints and formatting rules.
* Note special considerations for edge cases or null values.

---

## Example

* Provide a formal example of how to set or modify the variable using `tmux` commands.
* Include both short-form and long-form syntaxes if applicable.

---
